# Optional. Without this, the demo uses mocked Figma frame data.
FIGMA_ACCESS_TOKEN=

# Optional. Without this, the demo uses a deterministic mock generation.
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-sonnet-4-6.
ANTHROPIC_MODEL=claude-sonnet-4-6
